import { View, Text } from 'react-native'
import React from 'react'

type Props = {}

const index = (props: Props) => {
  return (
    <View className="h-['100%'] flex justify-center items-center bg-3-DarkMuted">
      <Text className="text-gray-50">index</Text>
    </View>
  )
}

export default index
